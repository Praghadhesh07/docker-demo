import React from 'react';

export default function Docker() {
  return <h1>Docker is used to build, package, and run applications in containers. It can fetch source code or prebuilt images from repositories like GitHub or Docker Hub.</h1>;
}
